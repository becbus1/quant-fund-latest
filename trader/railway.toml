[build]
builder = "Dockerfile"

[deploy]
numReplicas = 1
startCommand = "uvicorn trader.app.main:app --host 0.0.0.0 --port 8080"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
